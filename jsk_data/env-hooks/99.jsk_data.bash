#!/usr/bin/env bash
# -*- mode: Shell-script; -*-

# source 99.jsk_data.sh from same directory as this file
_THIS_DIR=$(builtin cd "`dirname "${BASH_SOURCE[0]}"`" > /dev/null && pwd)
. "$_THIS_DIR/99.jsk_data.sh"
